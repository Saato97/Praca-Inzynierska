version: '2'
services:
  esportsapp-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/esportsApp/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=esportsApp
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
